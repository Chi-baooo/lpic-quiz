// ここに、Firebaseコンソールの「プロジェクトの設定 → マイアプリ」で取得した
// あなた自身の設定値を貼り付けてください(README.mdの手順を参照)。
// apiKeyなどがこのファイルを通じて公開リポジトリに含まれても問題ありません。
// 実際のアクセス制御は firestore.rules 側で行われます(README参照)。

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
